const assert = require('assert');
const protodataDev = require('../index.js');

describe('protodataDev', function () {
  it('has a test', function () {
    assert(false, 'protodataDev should have a test');
  });
});
